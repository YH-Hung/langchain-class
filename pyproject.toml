[project]
name = "langchain-class"
version = "0.1.0"
description = "LangChain class project, managed with uv."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.9",
    "langchain-openai>=1.3.2",
    "python-dotenv>=1.2.2",
]
